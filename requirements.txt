# Version bounds keep installs reproducible and block surprise major-version
# upgrades that change APIs (ultralytics and mediapipe do this between majors).
opencv-python>=4.8,<5
mediapipe>=0.10,<0.11
scenedetect[opencv]>=0.6,<0.7
ultralytics>=8.3,<9
requests>=2.31,<3
tqdm>=4.66,<5
numpy>=1.24,<2
